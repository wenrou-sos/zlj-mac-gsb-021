# PostgreSQL connection (defaults match docker-compose / local dev)
PGHOST=localhost
PGPORT=5432
PGUSER=dcadmin
PGPASSWORD=dcpass
PGDATABASE=dc_capacity

# HTTP port
PORT=3000

# Set to 1 to insert demo racks & devices on startup when the racks table is empty
SEED_DEMO=1
