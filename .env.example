# PostgreSQL（server 容器读取）
PGHOST=postgres
PGPORT=5432
PGUSER=dcadmin
PGPASSWORD=dcpass
PGDATABASE=dc_capacity

# API 监听端口
PORT=3000

# 存储模式：postgres（默认）或 memory（无需数据库的演示模式）
DB_MODE=postgres

# Vite 开发服务器代理目标
VITE_API_TARGET=http://localhost:3000
